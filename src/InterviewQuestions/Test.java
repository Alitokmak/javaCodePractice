package InterviewQuestions;
//For a list and target sum, write function that returns
// zero-based indices of any two distinct elements whose sum is
// equal to the target sum. If there are no such elements,
// the function should return null. Example,
// FindTwoSum(new List<int>() { 2, 5, 3, 6, 12 }, 15)
// should return a Tuple<int, int> containing any of the following pairs
// of indices: 2 and 4 (3 + 12 = 15)


/*public class TwoSum {
public static int[] findTwoSum(int[] list, int sum) {
    if (list == null || list.length < 2) return null;
    for (int i = 0; i < list.length - 1; i++) { //lower indexed element
        for (int j = i + 1; j < list.length; j++) { //higher indexed element
            if (list[i] + list[j] == sum) {
                return new int[]{i, j};
            }
        }
    }

    //none found
    return null;
}
public static void main(String[] args) {
    int[] indices = findTwoSum(new int[] { 1, 3, 5, 7, 9 }, 12);
    System.out.println(indices[0] + " " + indices[1]);
}*/
import java.util.*;

public class Test {
    public static void main(String[] args) {

    }
}




